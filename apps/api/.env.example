# PostgreSQL (required from Phase 3a)
DATABASE_URL=postgresql://docgenerator:docgenerator@localhost:5432/docgenerator

# Anthropic (Phase 4a — filled mode)
ANTHROPIC_API_KEY=

# Optional: public API URL for apps/web integration (Phase 6)
# NEXT_PUBLIC_API_URL=http://localhost:3001
