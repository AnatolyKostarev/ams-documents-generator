# FSD layer: views (page compositions)

В Next.js App Router каталог `src/pages` зарезервирован под Pages Router.
Слой композиции страниц из FSD (`pages`) размещён здесь как **`views`**.

Маршруты остаются тонкими в `src/app/*` и импортируют компоненты из `@/views/...`.
